<%= form_for(@socialize_plan) do |f| %>
  <% if @socialize_plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@socialize_plan.errors.count, "error") %> prohibited this socialize_plan from being saved:</h2>

      <ul>
      <% @socialize_plan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :identifier %><br />
    <%= f.text_field :identifier %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
